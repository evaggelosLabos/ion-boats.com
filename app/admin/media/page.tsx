import MediaManagerClient from "./MediaManagerClient";

export const metadata = {
  title: "Media Manager - ION Boats Admin",
};

export default function AdminMediaPage() {
  return <MediaManagerClient />;
}
